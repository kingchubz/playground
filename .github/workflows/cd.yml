name: Deploy to AWS

on: workflow_dispatch

jobs:
    CD:
      runs-on: ubuntu-latest

      env:
        IMAGE_TAG: ${{ github.sha }}

      steps:
        - name: Checkout code
          uses: actions/checkout@v6

        - name: Debug secrets
          run: |
            if [ -z "${{ secrets.AWS_ACCESS_KEY_ID }}" ]; then echo "Missing ACCESS KEY"; fi
            if [ -z "${{ secrets.AWS_SECRET_ACCESS_KEY }}" ]; then echo "Missing SECRET KEY"; fi
            if [ -z "${{ secrets.AWS_REGION }}" ]; then echo "Missing REGION"; fi

        - name: Set up Python
          uses: actions/setup-python@v5
          with:
            python-version: "3.12"

        - name: Install dependencies
          run: |
            cd backend
            pip install -r requirements.txt

        - name: Configure AWS credentials
          uses: aws-actions/configure-aws-credentials@v4
          with:
            aws-region: ${{ secrets.AWS_REGION }}
            aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
            aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

        - name: Login to ECR
          run: |
              aws ecr get-login-password \
              | docker login \
              --username AWS \
              --password-stdin \
              ${{ secrets.ECR_REPOSITORY }}

        - name: Build image
          run: |
            cd backend
            docker build -t ${{ secrets.ECR_REPOSITORY }}:$IMAGE_TAG .

        - name: Push image to ECR
          run: |
            docker push ${{ secrets.ECR_REPOSITORY }}:$IMAGE_TAG

        - name: Upload static
          run: |
            aws s3 sync frontend s3://${{ secrets.S3_BUCKET_NAME }}/

        - name: Setup SSH
          run: |
            mkdir -p ~/.ssh
            echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/key.pem
            chmod 600 ~/.ssh/key.pem

        - name: Deploy container
          run: |
            ssh -o StrictHostKeyChecking=no \
            -i ~/.ssh/key.pem \
            ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}
            
            docker pull ${{ secrets.ECR_REPOSITORY }}:$IMAGE_TAG
            
            docker stop playground_app || true
            docker rm playground_app || true
            
            docker run --env-file /home/ubuntu/.env \
              ${{ secrets.ECR_REPOSITORY }}:$IMAGE_TAG \
              python manage.py migrate
            
            docker run -d \
              --name playground_app \
              --env-file /home/ubuntu/.env \
              -p 8000:8000 \
              ${{ secrets.ECR_REPOSITORY }}:$IMAGE_TAG
